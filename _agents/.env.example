# Anthropic API Key
# If you've already authenticated with Claude Code (`claude` command), this is optional
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: Default project ID
DEFAULT_PROJECT_ID=dr.melaxin

# Optional: Working directory (defaults to parent directory)
# WORKING_DIR=/Users/hantaku/Downloads/AP
